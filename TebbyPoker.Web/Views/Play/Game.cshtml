@model TebbyPoker.Web.Models.PlayGameViewModel
@{
    ViewBag.Title = "Game";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Start</h2>

@foreach (var player in Model.Players)
{
    @Html.Label("Player: ")
    @player.Name
    <br />
    @Html.Label("Hand: ")
    <br />

    foreach (var card in player.Hand)
    {
        @card.ToString()
        @Html.Raw("\n")
        @Html.Raw("\n")
    }
    <br />
    <br />
}